{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -u -c \"import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'pesticide_tracker.settings'); print('Loading Django...'); import django; django.setup(); print('Django ready!')\" && gunicorn --bind 0.0.0.0:${PORT:-8000} --workers 1 --timeout 120 pesticide_tracker.wsgi:application",
    "healthcheckPath": "/api/health/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
